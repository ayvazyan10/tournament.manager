{% extends 'layout/base.html.twig' %}

{% block title %}Создание турнира{% endblock %}

{% block content %}
    <div class="container mt-5">
        <h1 class="mb-4">Создание турнира</h1>

        <div class="card">
            <div class="card-body">
                {{ form_start(form) }}
                <div class="form-group">
                    {{ form_label(form.name, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.name) }}
                </div>
                <div class="form-group">
                    {% for child in form.teams %}
                        <div class="custom-checkbox-wrapper">
                            {{ form_widget(child, {'attr': {'class': 'custom-checkbox'}}) }}
                            {{ form_label(child, null, {'label_attr': {'class': 'custom-label'}}) }}
                        </div>
                    {% endfor %}
                    {{ form_errors(form.teams) }}
                    <small class="form-text text-muted">
                        Если не выбраны команды, будут добавлены все доступные команды в турнир.
                    </small>
                </div>
                <button type="submit" class="btn btn-success mt-3">Создать</button>
                {{ form_end(form) }}
            </div>
        </div>

        <a href="{{ path('tournament_index') }}" class="btn btn-secondary mt-3">Назад к списку турниров</a>
    </div>
{% endblock %}
